@model List<ShopBooks.Models.ChuDe>

<ul class="nav nav-list">
    @foreach (var item in Model)
    {
        <li>
            <a href="@Url.Action("CategoryBook", "Home", new { @macate = item.MaChuDe })">
                <span class="icon-chevron-right"></span>
                @item.TenChuDe
            </a>
        </li>
    }
    <li style="border:0">&nbsp;</li>
</ul>
